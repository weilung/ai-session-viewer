# 參與貢獻

歡迎回饋！這份工具是為了讓大家方便翻閱 AI session 紀錄，任何 bug、想法、改善都很歡迎。

## 這個 repo 是「投影」

本公開 repo 的原始碼是從維護者的**上游開發 repo 自動投影（同步）**而來，
**不會在此直接手改**。因此：

- 直接對本 repo 手動改檔／合併的變更，會在下次同步時被覆蓋。
- 這裡的檔案集是自動產生的公開子集（工具、文件、測試）。

## 怎麼貢獻

- **回報 bug／提功能建議** → 開一個 **Issue**（最主要的管道）。請附：你的 OS、Python 版本、
  重現步驟、預期 vs 實際結果。
- **想直接改碼** → 歡迎開 PR 展示你的做法，但請理解本 repo 是投影：你的變更會由維護者
  **搬進上游、驗證後重新投影**落地（你的 PR 可能以「已於上游採納」關閉，並在同步 commit 保留出處）。
  規模較大的改動，建議**先開 Issue 討論**方向再動手，避免白工。

## 本地測試

零外部相依，直接用 Python 3 標準函式庫：

```bash
python tests/test_smoke.py      # 煙霧測試（不含任何真實對話），成功印 OK
python tests/test_security.py   # 安全測試
```

## 隱私提醒

`out/` 是工具產生的輸出、**內含你的真實對話內容**，已被 `.gitignore`。
回報問題或附範例時，**請務必不要貼上真實私密對話**——用假資料或去識別化片段即可。

## 授權

送出貢獻即表示你同意以本專案的 **MIT License** 授權釋出。
