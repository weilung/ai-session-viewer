# 產生的輸出（內含你的私密對話內容，切勿提交！）
out/
out-*/

# Python
__pycache__/
*.py[cod]

# 編輯器 / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# AI 工具本機狀態
.claude/

# 跨模型 review 的暫存工作區（reviewer 唯一可寫的地方；fresh gate 前一律清空）
.review-tmp/

# 待人工確認後才套用的草稿，不提交
*.pending

# 書籤匯出檔（含使用者備註與 120 字對話摘要）——第 2 期才會產生，先擋著
/asv-bookmarks-*.json
/*bookmarks*.json
# ⚠ 錨定在 repo 根：未錨定的 `*bookmarks*.json` 會連 tests/fixtures/bookmarks.json
#    這種未來的測試素材一起靜默排除掉。匯出檔的實際落點是使用者的下載資料夾，
#    這裡要擋的是「順手拖進 repo 根」那一格。
